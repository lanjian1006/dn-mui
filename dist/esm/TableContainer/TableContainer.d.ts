import { ReactNode } from "react";
interface TableContainerProps {
    children?: ReactNode;
    title?: string;
    description?: string;
}
export default function TableContainer(props: TableContainerProps): JSX.Element;
export {};
